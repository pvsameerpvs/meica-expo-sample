const Tile = ({ label }) => (
  <div className="w-28 h-16 bg-white rounded-md flex items-center justify-center text-xs font-semibold text-center text-black shadow-sm">
    {label}
  </div>
);

export default Tile;
